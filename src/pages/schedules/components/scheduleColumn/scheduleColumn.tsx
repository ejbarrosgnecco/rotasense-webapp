import React, { useEffect, useState } from "react"

interface Timeframe {
    from: string,
    to: string
}

const ScheduleColumn: React.FC<{ timeframe: Timeframe }> = ({ timeframe }): JSX.Element => {
    

    return (
        <React.Fragment>
            hi
        </React.Fragment>
    )
}

export default ScheduleColumn